# Samsung Gear 360 Camera Calibration Parameters
# Generated from Python calibration tool (v2 - per-lens parameters)

[lens1]
# Lens center (normalized 0-1)
center_x = 0.51
center_y = 0.5001

# Polynomial distortion coefficients: r = f*θ*(1 + p1*θ + p2*θ² + p3*θ³ + p4*θ⁴)
p1 = 0.1
p2 = -0.0048
p3 = 0.0
p4 = 0.0

# FOV scale (1.0 = 180°, actual value corresponds to effective FOV)
fov = 1.3

# Per-lens rotation (degrees)
rotation_yaw = 50
rotation_pitch = 0.0
rotation_roll = 0.0

# Per-lens alignment offset (normalized 0-1)
offset_x = 0.0
offset_y = 0.0

[lens2]
# Lens center (normalized 0-1)
center_x = 0.5010
center_y = 0.4984

# Polynomial distortion coefficients
p1 = 0.0000
p2 = -0.0129
p3 = 0.0
p4 = 0.0

# FOV scale
fov = 1.0653

# Per-lens rotation (degrees)
rotation_yaw = -1.00
rotation_pitch = 0.80
rotation_roll = -0.40

# Per-lens alignment offset (normalized 0-1)
offset_x = 0.0
offset_y = 0.0140

[metadata]
# Lens FOV in degrees (physical lens specification)
lens_fov_deg = 195

# Calibration source
calibration_method = "procedural_tracking_v2"
frames_processed = 100
